<!doctype html>
<html lang="pt-br" data-theme="dark">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Listar OS (staging)</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script defer src="/static/js/theme.js"></script>
</head>
<body>
<nav class="navbar navbar-expand-lg bg-dark navbar-dark px-3">
  <a class="navbar-brand fw-semibold" href="/">T.I. Extremo — OS</a>
  <div class="ms-auto d-flex gap-2">
    <a class="btn btn-outline-light btn-sm" href="/os/nova">Nova OS</a>
    <a class="btn btn-outline-light btn-sm" href="/os/listar">Listar OS</a>
    <button id="theme-toggle" class="btn btn-outline-light btn-sm" onclick="toggleTheme()">Tema</button>
  </div>
</nav>

<main class="container py-4">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="h4 m-0">Ordens de Serviço</h1>
    <a class="btn btn-sm btn-primary" href="/os/nova">Nova OS</a>
  </div>

  <div class="table-responsive">
    <table class="table table-striped align-middle">
      <thead class="table-dark">
        <tr>
          <th>ID</th><th>Código</th><th>Cliente</th><th>Equipamento</th><th>Aberta em</th><th class="text-end">Ações</th>
        </tr>
      </thead>
      <tbody>
      {% for os in os_list %}
        <tr>
          <td>{{ os.id }}</td>
          <td>{{ os.os_code }}</td>
          <td>{{ os.cliente }}</td>
          <td>{{ os.equipamento }} {{ os.modelo }}</td>
          <td>{{ os.created_at }}</td>
          <td class="text-end">
            <a class="btn btn-sm btn-success" href="/os/ver/{{ os.id }}">Abrir</a>
            <a class="btn btn-sm btn-warning" href="/os/editar/{{ os.id }}">Editar</a>
            <form method="post" action="/os/excluir/{{ os.id }}" class="d-inline" onsubmit="return confirm('Confirma excluir esta OS?');">
              <button class="btn btn-sm btn-danger">Excluir</button>
            </form>
          </td>
        </tr>
      {% endfor %}
      </tbody>
    </table>
  </div>
</main>
</body>
</html>
